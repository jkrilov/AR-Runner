# Session: v0.5.20 shipped — release-guard tag-push path validated end-to-end

**Date:** 2026-05-27T12:01:02Z  
**Topic:** v0.5.20 release-guard chore shipped; tag-push auto-trigger path validated  
**Key Outcome:** First pre-release in project history to traverse tag-push cleanly (v0.5.5–v0.5.19 all required `workflow_dispatch` fallback)

## Event Summary

PR #117 fixed two bugs in `release-testflight.yml`:
1. Self-collision on tag-push (trigger tag included in candidates, self-rejected)
2. SemVer misordering (sort -V incorrectly ranks pre-releases after releases)

Fix: inline `semver_gt` bash function + trigger-tag exclusion + self-test step (11 fixtures).

Tag `v0.5.20-1` pushed → workflow run 26511705252 → guard PASS → build 50 shipped to TestFlight.

## Impact

All future v0.5.x and v0.6.x pre-release tag-pushes now auto-trigger TestFlight reliably. Release checklist bumped to medium confidence (single real-world validation; will reach high after next pre-release confirms).
