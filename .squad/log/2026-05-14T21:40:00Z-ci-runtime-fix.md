# Session Log — CI Runtime Fix & Fresh Eyes

**Timestamp:** 2026-05-14T21:40:00Z  
**Session:** ci-runtime-fix  
**Agent:** Amber  
**Branch:** chore/ci-workflows

## Session Summary

Amber rotated in to diagnose CI regression post-Richards's `-downloadPlatform` attempt. Initial triage showed 3/5 failures. Root cause investigation revealed the macos-15 runner image under Xcode 16.0 lacks iOS 18.0 + watchOS 11.0 simulator runtimes despite shipping the SDKs — a classic simulator-runtime gap.

## Key Insight

Fresh agent perspective cut through two layers of misinterpretation:
- Richards and coordinator initially read the destination-spec error as a workflow config issue.
- Actual cause: no simulator runtime installed → xcodebuild's enumeration fallback.

Reading the full `Ineligible destinations:` block (not just the `name:` field) revealed the diagnosis.

## Fix

Pin Xcode 16.4 (ships iOS 18.5 + watchOS 11.5 pre-baked). No more `-downloadPlatform` step.

## Lesson

Routing CI issues to a different agent when one attempt regressed 3→5 failures paid off immediately. Fresh eyes see through established narratives fast. Document the macos-15 runner-image catalog reference for next person who touches the runner pins.

## Outcome

✅ Green CI, clean commit, all regressions eliminated.
