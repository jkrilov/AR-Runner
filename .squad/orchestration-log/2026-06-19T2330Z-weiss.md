# 2026-06-19T23:30Z — Weiss: Custom HUD Layout Planning

## Task

Plan rendering + data model for custom HUD layout editor (v0.6.1 phone UI, constrained-custom verdict).

## Outcome

**PLANNING COMPLETE — No code changes.**

Established Core data types (`HUDLayoutCatalog`, `WorkoutLayoutDefaults`) and watch apply path:
- Reuse `HUDLayout` unchanged (already Codable, carries slots)
- `standard4` only in v1 (4 fixed slots, bench-validated coords)
- Swap watch hardcoded default with resolver chain (defaults → catalog → fallback)
- `HUDLayout.validated(for:)` sanitizes invalid metrics at apply time
- Slot-1 overflow risk identified + mitigation (editor warning + 0.5-day bench spike)

Dependencies on Richards (persistence/sync), Killian (UX), Laughlin (watch implementation).

## Open Questions

- v1 = standard4 only?
- Soft warn vs hard-block slot-1 overflow?
- Custom-icon support deferred?
- Per-type scoped or global layouts?

## Related

- Decision: `.squad/decisions.md` § "Weiss: Custom HUD Layout Editor — Glasses Rendering + Data-Model Plan"
- HUD planning session: `.squad/log/2026-06-19-custom-hud-planning.md`
