# Laughlin-1 Spawn — Swift/watchOS/HealthKit Audit

**Timestamp:** 2026-05-17T00:22:43Z (UTC)  
**Model:** claude-opus-4.7-1m-internal  
**Mode:** Background (completed)  
**Scope:** ARRunnerWatch, ARRunnerPhone, ARRunnerWidgets; watchOS-touching parts of ARRunnerCore. Excludes BLE (Weiss) and CI (Richards).  
**Branch:** main

## Outcome

✅ **Completed.** Audit doc written to `.squad/audits/2026-05-16-laughlin-watchos.md`.

### Key Findings

1. **Two real bugs:**
   - HealthKit energy samples mis-classified as `.duration` in `metric(for:)` — live kcal never reaches view-model.
   - `StartWorkoutIntent.perform()` is a no-op TODO — widget Start button doesn't launch the run flow.

2. **Swift 6 strict concurrency:** Solid. `WorkoutController` is a proper actor, no force-unwraps, `@Observable` used correctly.

3. **SwiftUI modernization:** No legacy patterns. `@Observable`, `NavigationStack`, modern API surface throughout.

4. **Three architectural debt items:**
   - Deprecated HK aggregate properties (`totalDistance`, `totalEnergyBurned`) at lines 193–194.
   - Optional-pattern match on `HKQuantityType.quantityType(forIdentifier:)` can silently skip.
   - Preview constructs real `HealthKitWorkoutSubstrate` (risky for eager-init future).

### Output Reference

`.squad/audits/2026-05-16-laughlin-watchos.md` — 15640 bytes, 40+ code references.

### Decision Inbox Dropped

`laughlin-audit-hk-deprecated-api.md` — HealthKit deprecated properties + MetricKind bug (effort S).
