# Orchestration Log — Weiss

**Date:** 2026-06-18T11:56Z  
**Agent:** Weiss (AR Integration)  
**Session:** 0.6.0 release build-out  
**Branch:** `fix/activelook-dormant-layout-cmds`

## Summary

Fixed two dormant ActiveLook BLE command bugs in the curated-layout path (disabled in production since v0.3, but de-risked before it ships in v0.6.x).

**Scope:** ARRunnerCore BLE encoder + ActiveLookGlassesAdapter integration layer.

## Decisions Made

1. **Phantom `widgetUpdate` (0x3A) removed** — command ID does not exist in ActiveLook spec; replaced with spec-correct `layoutClearAndDisplay(0x69)` for per-tick text updates.
2. **`displayLayout` (0x62) NUL-termination** — encoder now appends `[id, text, 0x00]` per spec §4.9 / §5.11 (was `[id]` only). Activation frames now spec-compliant.

## Test Results

**218 tests executed, 1 skipped, 0 failures** on `swift:6.0-jammy`.  
Added byte-exact encoder tests for `layoutClearAndDisplay(id:text:)` and `displayLayout(id:text:)` (empty/non-empty variants).  
Regression guard: no command ID maps to `0x3A`.

## Blockers Resolved

None. Both commands disabled in production; no live caller impact.

## Follow-up Work

- **Weiss (0.6.1):** Phone custom-layout editor UI (depends on feasibility study locked).
- **Weiss (0.6.x integration):** Render the per-type `HUDLayout.default(for:)` via parameterized raw-`txt` path (existing, proven rc16).

## Notes

The raw-`txt` HUD path (`RunningHUDFrame` → `sendCommands`) remains untouched and is the active code path for v0.5.x+. These fixes prevent regression class of bugs when the curated path is re-enabled in v0.6.1 with custom layouts.
