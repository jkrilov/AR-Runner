# Weiss-1 Spawn — ActiveLook / BLE / AR HUD Audit

**Timestamp:** 2026-05-17T00:22:43Z (UTC)  
**Model:** claude-opus-4.7-1m-internal  
**Mode:** Background (completed)  
**Scope:** ActiveLook, CoreBluetooth, AR HUD integration. Excludes watchOS app shell (Laughlin) and CI (Richards).  
**Branch:** main  
**Note:** Merged pass incorporating earlier Sonnet findings on CBCentralManager re-instantiation and RSSI drift.

## Outcome

✅ **Completed.** Audit doc written to `.squad/audits/2026-05-16-weiss-ar-ble.md`.

### Key Findings

1. **BLE wrapper is architecturally sound** — pure-Swift encoder in Linux-buildable Core, actor owns mutable state, Coordinator bridges CB delegate boundary on dedicated queue, D4 auto-reconnect with backoff implemented.

2. **Critical integration gap:** Per-tick HUD field-update path is **not wired** into workout pipeline. Glasses get initial layout activation then go silent for the rest of the workout.

3. **Secondary concerns:**
   - `CBCentralManager` re-instantiated on every reconnect attempt (up to 30 stale managers per workout).
   - Untracked scan-timeout task can fire spuriously across reconnect cycles.
   - Discarded `controlChar` / RSSI notifications.

4. **Placeholder device IDs:** `CuratedLayoutCatalog.mapping` uses 0x01/0x02/0x03 (placeholders). Real IDs depend on Config-Generator output (D6).

### Output Reference

`.squad/audits/2026-05-16-weiss-ar-ble.md` — 17157 bytes, 50+ code references.

### Decision Inbox Dropped

`weiss-audit-layout-bake-blocking.md` — Layout presets bake blocking hardware use (decision D6 expansion, owner Joe + Weiss).
