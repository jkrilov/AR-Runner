# Richards Orchestration Log — 2026-05-16T20:32:00Z

**Agent:** Richards (Lead / Architect)  
**Task:** TestFlight CI end-to-end design + implementation  
**Status:** Completed  
**Deliverable:** PR #21

## Outcome

Richards designed and implemented the entire TestFlight CI pipeline from scratch:

- **Workflow:** `.github/workflows/release-testflight.yml` — native xcodebuild + ASC API key, tag-driven, serial concurrency
- **Signing config:** `Config/Signing.xcconfig` (gitignored) + `scripts/bootstrap-signing.sh` (idempotent bootstrapping)
- **Version strategy:** MARKETING_VERSION from tag; CURRENT_PROJECT_VERSION from `$GITHUB_RUN_NUMBER`
- **Project updates:** `project.yml` with configFiles reference for Signing.xcconfig
- **Documentation:** `docs/dev/testflight-setup.md` (Part A: Apple Developer Portal steps; Part B: 7 secrets + copy-paste CLI)
- **Skill:** `.squad/skills/ios-testflight-ci-via-actions/` (reference implementation)

## Key Decisions (locked in decisions.md)

- **D-RICHARDS-TF-1:** xcodebuild -allowProvisioningUpdates over fastlane match (simpler for single dev)
- **D-RICHARDS-TF-2:** Tag pattern `v*.*.*-*` for TestFlight; `v*.*.*` reserved for App Store
- **D-RICHARDS-TF-3:** Version from tag; build number from GitHub run ID (monotonic)
- **D-RICHARDS-TF-4:** Signing team ID in gitignored xcconfig, bootstrapped via script
- **D-RICHARDS-TF-5:** 7 GitHub Secrets (team ID, ASC API credentials, cert + password, keychain password)
- **D-RICHARDS-TF-6:** Serial concurrency (no cancel-in-progress) to protect ASC upload
- **D-RICHARDS-TF-7:** Xcode 16.4 / macos-15 for reproducibility

## Next Milestone (Joe's responsibility)

Before first workflow run:
1. Register 4 bundle IDs at developer.apple.com
2. Create App Store Connect app record for `com.arrunner.phone`
3. Create + export Apple Distribution cert
4. Create ASC API key (role: App Manager), download .p8
5. Add 7 secrets to GitHub repo settings
6. Merge PR #21
7. Tag first pre-release: `v0.2.0-rc1`

## Notes

- iPhone is not in developer mode, so TestFlight is the testing path.
- v0.2 watch app is functionally complete in simulator with HealthKit.
- App Store submission flow (v1) is out of scope; separate PR later.
