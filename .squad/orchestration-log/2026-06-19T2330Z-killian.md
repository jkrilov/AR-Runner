# 2026-06-19T23:30Z — Killian: Phone Custom HUD Editor UX Planning

## Task

Plan phone Settings editor UX for custom HUD layout creation + per-type default assignment (v0.6.1, Phase B).

## Outcome

**PLANNING COMPLETE — No code changes.**

Designed buildable v1 feature:
- Settings → "Glasses Layouts" disclosure row (new section between Workout and Units)
- Three sections: read-only presets, custom CRUD, per-type default assignment (6 rows)
- Fixed 4-slot editor (2×2 visual grid, empty slots allowed)
- Tap-slot → modal metric picker (grouped valid/unavailable, prevent duplicates)
- Auto-name from filled slots, user override allowed
- Static synthetic-value amber preview (304×256 aspect, approximate)
- Validity = warn-and-allow (not block); watch renders `--` for invalid/missing
- Apply at next workout, never mid-run

Minimum lovable v1 scope vs deferred (v0.7+) features clearly separated.

Dependencies on Richards (Phase A persistence/sync), Weiss (grid validation), Laughlin (phone implementation).

## Open Questions

- Fixed 4-slot (recommend yes)?
- Unlimited or capped custom layouts?
- Auto-name-with-override or require name?
- Warn-and-allow or hard-block invalid metrics?
- Global layout or per-type instances?
- Approximate preview or pixel-exact?
- Per-type + global reset affordances?
- Settings row or dedicated tab?

## Related

- Decision: `.squad/decisions.md` § "Killian: Phone Custom HUD Layout Editor UX"
- HUD planning session: `.squad/log/2026-06-19-custom-hud-planning.md`
