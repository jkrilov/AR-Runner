# Orchestration Log — Amber

**Date:** 2026-06-18T11:56Z  
**Agent:** Amber (QA/Fitness Domain)  
**Session:** 0.6.0 release build-out  
**Branch:** `feat/0.6.0-core-foundation`

## Summary

Fitness-domain analysis and test strategy for 0.6.0 multi-sport correctness. Identified 11 correctness risks (R1–R11) across metric validity, formatting, and HealthKit substrate gaps. Delivered recommendations for test coverage: `SportTypeExtendedTests`, `MetricValidityTests`, `MetricFormattingExtendedTests`, `HUDLayoutDefaultTests`, `MetricLayoutValidationTests`, `EnergyEstimatorCrossSportTests`, and `WCMessageSchemaVersionTests`.

**Scope:** ARRunnerCore test strategy; device bench coordination for indoor distance, cadence sensor, elevation indoors, energy reconciliation.

## Decisions Made

1. **Metric validity matrix** — pace ✅ run/walk, ❌ cycling (speed required instead); elevation ✅ outdoor, ❌ indoor.
2. **Cadence semantics** — steps/min for running, RPM for cycling; formatter must branch.
3. **`MetricKind.isValid(for: SportType)`** — validator API to catch invalid metric/sport combos at edit time.
4. **EnergyEstimator cross-sport** — Keytel formula validated for running; walking/cycling ranges must be plausible.

## Test Plan Priorities

1. **Unit tests (pre-device):** SportType Codable, metric validity, formatting branches, HUDLayout defaults validation, WC schema compat.
2. **Device bench:** indoor distance accuracy, cadence sensor presence/absence, elevation noise indoors, energy kcal reconciliation.

## Blockers Resolved

None. All metrics/formatters designed by Richards (Core) and Laughlin (app shells); Amber owns validation strategy.

## Follow-up Work

- **Amber (post-PR-#121):** Write all Core unit tests (11+ test files), validate CI green.
- **Amber (device bench):** Indoor distance, cadence sensor, elevation indoors, energy cross-sport.
- **Laughlin (phone editor):** Validate metric/sport compatibility on save (uses `MetricKind.isValid` API).

## Notes

- TCX sport field (Richards' TCX encoder) mapped per ActivityKind to standard Strava strings.
- Phase 1 (v0.6.0) scope: data model + type picker + defaults. Phase 2 (v0.6.1): custom-layout editor + constraint validation.
