# Orchestration Log — richards-1 (Lead)

**Time:** 2026-05-19T22:25:00Z  
**Duration:** 169 seconds  
**Task:** ADR — BLE link lifecycle (user-managed, not workout-scoped)

## Dispatch

- **Intent:** Formalize the BLE-link contract for rc17 and beyond. Codify the "workout-stop does NOT disconnect glasses" design decision already partially implemented in rc16.
- **Input:** Joe's rc16 bench report ("connection drops on workout stop"), rc17 team kickoff, Weiss's adapter spike findings, Laughlin's finish-screen work.
- **Deliverable:** `richards-adr-ble-link-lifecycle.md` (canonical ADR for v0.4+).

## Work Done

1. **ADR authored:** 89 lines covering lifecycle invariants (I1–I4), tear-down rules (R1–R5), reconnect policy (P1–P5), subscription lifecycle (S1–S4), phone-optional implication (PO1–PO3), rationale, trade-offs considered, and implications for each team member.
2. **Canonical decision:** BLE link is a **user-managed peripheral session**, independent of workout state. Only two code paths permitted to invoke `transport.disconnect()`:
   - User-facing `disconnectGlasses()` action (settings/idle surface).
   - System error recovery when CoreBluetooth state is unrecoverable.
3. **Contract pinned:** Finish-screen persistence, auto-reconnect backoff schedule, per-link subscription pattern, phone-optional invariants all formalized.
4. **Cross-team assignments:** Weiss (adapter audit + reconnect backoff), Laughlin (regression test + deletion of `teardownTransport()` from workout paths), Amber (battery metric sourcing), Tests (new DisconnectResilienceTests cases).

## Output

- **File:** `.squad/decisions/inbox/richards-adr-ble-link-lifecycle.md`
- **Status:** Accepted (Scribe merged to decisions.md at 2026-05-19T22:25Z).
- **Related artifacts:** `paired-hardware-lifecycle-contract` skill (new), updated `activelook-ble-adapter-pitfalls` skill.

## Next

- Weiss implements P1–P5 (reconnect backoff) and audits adapter disconnect paths.
- Laughlin removes `teardownTransport()` from workout-stop paths and adds regression tests.
- Amber sources battery characteristic per per-link rule.
