### 2026-05-20T14:50:00Z — Laughlin (watchOS) — rc2 implementation (PR #79)

**Agent:** laughlin  
**Model:** claude-opus-4.7-1m-internal  
**Duration:** 1079s (18+ minutes)  
**Status:** ✅ Complete / Ready for CI

**Objective:** Implement rc2 fixes covering 5 items from Joe's bench feedback. Deliver PR-ready watchOS app update.

**Implementation Summary:**

**Item #1 — GPS Route Recording (CLLocationManager + HKWorkoutRouteBuilder):**
- Added `NSLocationWhenInUseUsageDescription` to watch Info.plist via `project.yml` properties (config-driven, not hand-edited)
- Wired `CLLocationManager` into `HealthKitWorkoutSubstrate` with `kCLLocationAccuracyBest`, `activityType = .fitness`, `distanceFilter = kCLDistanceFilterNone`
- Created `HKWorkoutRouteBuilder` at `begin(...)`, ingested location fixes (filtered: horizontalAccuracy > 50m or < 0), finalized at `end(...)` attaching polyline to `HKWorkout`
- **Discard path explicitly skips route finalize** — samples drop with discarded workout (no leakage)

**Item #3 — Finish-Screen Layout Reshape (3-line / 4-data, font 2 line 3):**
- New layout: Finished! / distance / time+pace (vs. rc1 banner / distance / time)
- Y constants unchanged (239/151/63 = finishLine1Y/2Y/3Y); renamed from rc17 (backward-compat aliases added)
- Line 3 uses font 2 (~18 px/char) vs. font 3 (~28 px/char) to fit two metrics in 304 px width
- Extracted `ALookFontMetrics` per Richards's rc13 recommendation (heights from docs, widths empirical)
- 5 new tests covering Y formula, no-overlap invariant, render correctness, string length bounds

**Item #4 — Discard-vs-Save Data-Integrity (Terminal-Path Bifurcation) — HIGHEST SEVERITY:**
- Root cause: `confirmCancel` was calling `end()` which always persisted `HKWorkout` regardless of user intent
- **Fix:** Split save and discard onto distinct protocol methods
  - New `WorkoutHealthSubstrate.discard(at:)` method (protocol + all implementations)
  - `HealthKitWorkoutSubstrate.discard()`: calls `session.end()` + `builder.discardWorkout()` (NO finishWorkout, NO route finalize)
  - `InMemoryWorkoutHealthSubstrate` + `FakeHealthKitSubstrate` record `.discard` distinct from `.end`
  - `WorkoutController.discard()` terminal method (no summary returned)
  - `WorkoutViewModel.confirmCancel()` now routes through `controller.discard()` not `end()`
- No "save then maybe delete" — a failed delete can no longer leak partial data into Health
- `WorkoutDiscardTerminalPathTests` pins the bug class: save → `substrate.end` called 1× NEVER discard; discard → `substrate.discard` called 1× NEVER end (regression catch)

**Item #5 — Phone-Mirror "Started at HH:MM" Row:**
- Added optional `startedAt: Date?` to `WorkoutTickMessage`, carried on every tick (phone joins mid-run sees value on first snapshot)
- WC schema bump **v3 → v4**; field optional for backward-compat (v3 snapshots decode on v4 phones, fallback to `timestamp − elapsedSeconds`)
- Phone-side: added "Started" row (SF Symbol checkered flag + `.short` DateFormatter) above metrics grid
- Round-trip + backward-compat pinned in `WorkoutTickMessageTests`

**Release Mechanics:**
- `project.yml`: bundle 32 → 33, MARKETING_VERSION stays 0.4.0 (fix-rc, not marketing bump)
- Info.plist `$(VAR)` placeholders preserved (skill gotcha)
- xcodegen regenerated once; `.pbxproj` deltas nominal for yml change + new ALookFontMetrics source
- Tests: 186 → 195 Core (+9 new tests)
- **xcodebuild ARRunnerWatch SUCCEEDED** (CI-ready)

**PR Metadata:**
- Branch: `rc2/v0.4.0-rc2-bench-feedback`
- PR: #79
- Files changed: 15
- Insertions: +983 | Deletions: −184
- Test coverage: All new feature paths instrumented; backward-compat round-trip tested

**Coordination Notes:**
- Weiss's rc2 finish-screen coordinate spec did not land pre-push (will comment on PR if Y math needs adjustment; Y constants unchanged from rc17 so risk surface limited to line 3 right-justify anchor)
- Richards's Strava diagnosis (item #2) — if HK metadata keys required, changes attach cleanly to substrate `end(...)` path (route already finalized there)
- Amber's terminal-path-data-leak-qa skill landed in `.squad/skills/` from parallel QA; rc2 confirmCancel→discard fix directly instantiates that pattern

**New Skill:** `hkworkoutroutebuilder-lifecycle-watchos` (CLLocationManager lifecycle coupling, route-builder finalization contract, discard semantics)

---

**Metadata:**
- Spawn time: 2026-05-20T10:42:31-04:00 (rc2 batch)
- Completion time: 2026-05-20T11:20:50-04:00 (1079s elapsed / 18 min)
- Input: Spawn manifest + Weiss coord spec (advisory) + Amber acceptance criteria + Richards Strava diagnostic
- Output: PR #79 ready for merge + 3 new skills + orchestration log
