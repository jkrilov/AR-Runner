# Orchestration Log — weiss (AR Integration)

**Time:** 2026-05-19T22:25:00Z  
**Duration:** 673 seconds  
**Task:** rc17 — BLE-adapter side of rc17 release. Battery filter. Reconnect policy.

## Dispatch

- **Intent:** Implement the BLE-link lifecycle ADR on the adapter side. Add battery characteristic subscription and filtering. Implement reconnect backoff policy.
- **Input:** Richards's ADR (canonical contract), Joe's rc16 bench report, Amber's QA scenarios (battery §C, reconnect §A7–A8), User directive 2026-05-19T18:20 (phone-optional).
- **Deliverable:** Core code + tests for `BatteryLevelFilter`, `ExponentialBackoff.adrV04`, adapter reconnect audit.

## Work Done

1. **`ExponentialBackoff.adrV04` (new, Core):** Approximates ADR backoff schedule 1s→2s→4s→8s→16s→32s→60s steady using pure-exponential math (matches the ADR's intent). `maxReconnectAttempts` changed from 30 to `.max` per ADR P2.
2. **`BatteryLevelFilter` (new, Core):** Gatekeeper for battery-level events:
   - Drops bytes > 100 (firmware glitches) with warning.
   - Suppresses identical consecutive percents (30 s notify cadence re-publishes most of the time).
   - `.reset()` on disconnect guarantees first post-reconnect read lands, UI transitions from "—" to fresh value.
3. **Adapter audit:** Confirmed only two paths invoke `transport.disconnect()`: user-explicit `disconnectGlasses()` and error-recovery on unrecoverable state. No further adapter changes needed (Laughlin + Amber handle their slices).
4. **Subscriptions:** Battery characteristic (0x180F / 0x2A19) wired as per-link subscription (S1–S4 pattern), not workout-scoped. Initial read forced on every `.connected` transition so first battery value lands within ~2 s instead of waiting 30 s.
5. **Test results:** 186/186 Core tests pass (+10 new for filter, backoff, schema v3). `ARRunnerWatch` build succeeded.

## Output

- **File:** `.squad/decisions/inbox/weiss-rc17-ble-lifecycle-and-battery.md`
- **PR:** Co-author on `#77` (v0.4.0-rc1), merged with Laughlin & Amber, CI green.
- **Status:** TestFlight upload queued (auto-release per Joe's directive).
- **Related artifacts:** `.squad/skills/activelook-ble-adapter-pitfalls/SKILL.md` updated (per-link pattern, filter-reset).

## Code Files Touched

- `ARRunnerCore/Sources/ARRunnerCore/Glasses/ReconnectPolicy.swift` (+adrV04)
- `ARRunnerCore/Sources/ARRunnerCore/Glasses/BatteryLevelFilter.swift` (new)
- `ARRunnerCore/Tests/ARRunnerCoreTests/Glasses/BatteryLevelFilterTests.swift` (new, 7 tests)
- `ARRunnerCore/Tests/ARRunnerCoreTests/Glasses/ExponentialBackoffTests.swift` (+adrV04 envelope test)
- `ARRunnerWatch/Glasses/ActiveLookGlassesAdapter.swift` (filter wired, defaults updated, reset on drop)

## Next

- Joe bench-validates rc17 on real hardware (TestFlight link from Apple).
- Amber runs QA scenarios §A–E (28 total items).
