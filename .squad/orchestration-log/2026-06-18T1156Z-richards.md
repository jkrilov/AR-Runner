# Orchestration Log — Richards

**Date:** 2026-06-18T11:56Z  
**Agent:** Richards (Lead/Architect)  
**Session:** 0.6.0 release build-out  
**Branch:** `feat/0.6.0-core-foundation`

## Summary

Delivered the Core foundation for 0.6.0: orthogonal `WorkoutType` (activity × environment composite struct), `UnitSystem` + unit-aware formatters, `MetricKind.speed`, `WorkoutSummary.averageSpeed`, `WCMessage` v6 with lenient decode, per-workout-type `HUDLayout.default(for:)`, and TCX sport mapping for indoor/outdoor variants.

**Scope:** ARRunnerCore + downstream implications for watch/phone/widget shells (owned by Laughlin).

## Decisions Made

1. **`WorkoutType` composite struct** — 6 cases (outdoor {run, walk, bike} × indoor {run, walk, bike}), custom Codable for backward compat on the wire (outdoor variants as "running"/"walking"/"cycling", new combos as "indoor_running" etc.).
2. **Schema v6 + lenient decode** — unrecognized message kinds decode to `.unknown` rather than throwing; hard version incompatibility still throws.
3. **`UnitSystem` in Core** — pure Swift, no Locale coupling. Parameterized formatters for pace/speed/distance/elevation.
4. **Metric correctness** — `.speed` metric, `averageSpeedMetersPerSecond?` on summary, per-type defaults (cycling → speed, indoor → no elevation).

## Test Results

**259 tests executed, 1 skipped, 0 failures** on `swift:6.0.3-jammy` (Linux CI mirror).  
All Core protocol boundaries preserved. No Apple frameworks imported.

## Blockers Resolved

None. All decisions locked by jkrilov pre-implementation.

## Follow-up Work

- **Laughlin:** Migrate watch `begin(sport:)` → `WorkoutType`, add type picker, wire settings sync (UnitSystem + defaultWorkoutType).
- **Weiss:** Render per-type `HUDLayout` defaults, including `.speed` metric.
- **Amber:** Integration coverage for indoor/outdoor HealthKit mapping, cross-version WC decode.

## Notes

No pre-existing code modified outside Core. SportType fully replaced in Core codebase; no deprecation shim issued (downstream shells own the migration).
