# 2026-06-19T23:30Z — Richards: Custom HUD Layout Persistence + Sync Architecture

## Task

Plan persistence + watch↔phone sync architecture for custom HUD layouts (v0.6.1 Phase A + Phase B).

## Outcome

**PLANNING COMPLETE — No code changes.**

Designed Phase A (persistence + sync, can ship BEFORE editor UI):
- Two new Core Codable types: `HUDLayoutCatalog` (user customs only), `WorkoutLayoutDefaults` (WorkoutType → id mapping)
- App Group UserDefaults store (keys: `"hudLayoutCatalog"`, `"workoutLayoutDefaults"`) via new `Shared/Settings/HUDLayoutStore`
- **Additive within WCMessage v6 (NOT v7 bump)** — adds `.layoutCatalog` + `.layoutDefaults` cases; v6 peers degrade gracefully to `.unknown`
- Full-catalog replace sync (not incremental), phone-authoritative (phone-edit-only in v0.6.1)
- Watch persistence: decode + write to App Group on receipt (enables offline resolve at next workout)
- Pure, Linux-testable Core resolver with dangling-reference fallback
- Max-cap proposal: 16 custom layouts

Phase B (editor UI) depends on Phase A complete.

## Open Questions

- CloudKit in 0.6.1 (recommend no)?
- Max custom layouts cap (propose 16)?
- 4-slot only confirmed?
- One-directional sync acceptable?
- Prune orphaned assignments on delete?
- Confirm additive-v6 (not v7)?

## Related

- Decision: `.squad/decisions.md` § "Richards: Custom HUD Layout Persistence + Watch↔Phone Sync Architecture"
- HUD planning session: `.squad/log/2026-06-19-custom-hud-planning.md`
